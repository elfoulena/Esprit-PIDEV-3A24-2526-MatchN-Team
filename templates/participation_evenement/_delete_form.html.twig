<form method="post" action="{{ path('app_participation_evenement_delete', {'id_participation': participation_evenement.id_participation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation_evenement.id_participation) }}">
    <button class="btn">Delete</button>
</form>
