<form method="post" action="{{ path('app_evenement_delete', {'id_evenement': evenement.id_evenement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.id_evenement) }}">
    <button class="btn">Delete</button>
</form>
